#include <bits/stdc++.h>
using namespace std;

int main() {
    int n,t;
    cin>>n>>t;
    bool cows[n];
    for(int i=0;i<n;i++) {

    }
}

// # patient zeros: go sequentially from start with set of all cows, remove if one infected and other not
// value of k: for all cows record first infect and last infect